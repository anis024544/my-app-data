import React from "react";
import ReactDOM from "react-dom";

const Son2 = ({headline}) => {
  return (
    <div>
      <h1>this is from son 2</h1>
      <p>{headline}</p>
    </div>
  );
};

export default Son2;
